---
name: rmrb-canary
description: |
  以人民日报为议程观察主轴、以原始政策文件为事实锚点的政策信号分析框架。
  当用户需要分析人民日报报道、解读政策信号、评估某行业的官方表述变化，
  或问"官方媒体怎么说""政策风向如何""这个信号是否异常"时使用。
  四种模式：政策分析（采集+分析）、只读审阅（不采集）、历史回放（指定 as_of）、
  报告写作（标准报告 / 董事长简报）。
---

# RMRB-Canary — 政策信号分析框架

## 这个 skill 做什么、不做什么

**做**：把公开报道放回版面、栏目、机构和历史变化中，说明**什么变了、依据是什么、
对谁有影响、下一步该看什么、什么证据会推翻当前判断**。

**不做**：不预测"多少天内会出台政策"。风险倒计时未经历史标定，本框架已取消。
不推断官方动机 —— 报告回答的是"公开表述、正式措施与可观察执行之间有哪些差异"，
不是"官方实际在想什么"。

## 四种模式（先确认在哪一种，再动手）

| 模式 | 触发 | 是否采集 | 产出 |
|---|---|---|---|
| 政策分析 | 用户问某议题的官方表述/政策信号 | 是 | 标准报告（references/report-standard.md） |
| 报告写作·简报 | 用户说"简报""日报""董事长版" | 复用已有分析结果 | 董事长简报（references/report-brief.md） |
| 历史回放 | 用户指定过去某日的判断 | 是，且必须传 `--as-of` | 标准报告 + 明确的截面声明 |
| 只读审阅 | 用户要求评估/审阅本 skill 或其结果 | **否** | 结论与问题清单，不启动采集 |

只读审阅不得自动启动采集。用户说"评估/分析/怎么看"时，先给结论，不改数据、不跑管道。

## 运行

管道是纯计算的（零 LLM 调用），在 **skill 安装根目录**下运行。不要写死终端目录：
同一套 skill 可能同时安装在多个客户端目录下，写死路径会让你跑到另一份副本上。

```bash
# 先切到本 SKILL.md 所在目录，再执行
python3 -m agent.agent --topic ai --enterprise example-enterprise
```

已配置的试点主题：`ai`（人工智能）、`vaccine`（疫苗）；企业画像模板：`example-enterprise`。
真实客户画像放在 `config/enterprises/<key>.json`，**不进版本库**（.gitignore 白名单）。
定义与暴露映射见 `references/topics.md`。`--list-topics` 可列出全部。

| 参数 | 含义 |
|---|---|
| `--topic` | 主题名（`config/topics/<name>.json`）。带口径版本，是正式时间线 |
| `--enterprise` | 企业画像名。不给则不做企业影响推导 |
| `--keyword` | 临时关键词口径，口径版本为 `adhoc`，**不与正式主题历史合并** |
| `--date` | 采集哪一期，YYYYMMDD |
| `--as-of` | **分析截止时点**。历史回放必须显式指定：只使用该时点前可见的证据与历史记录 |
| `--skip-media` | 跳过外部媒体采集 |
| `--skip-documents` | 跳过正式政策文件采集 |
| `--no-cache` | 禁用共享文档缓存（强制重抓） |
| `--dry-run` | 只读演练：不写分析、告警、档案、提法记录或共享缓存 |
| `--compact` | 精简输出（去掉原文与文章列表） |
| `--history` | 列出当前算法版本的历史记录 |

`--brief` **不是** CLI 参数。简报是报告写作模式，由自然语言触发。

批量运行多个主题（共享同一份文档缓存，第二个主题不再重抓全报）：

```bash
python3 -m agent.runner --topics ai vaccine --enterprise example-enterprise
```

```bash
python3 -m agent.runner --pending
```

**运行器不含调度器，也不会自己跑起来。** 后台作业须先登记 `AUTOPILOT.md`
并经用户确认才能启动；当前没有任何后台作业在运行。

历史回放（每期 `as_of` 设为当天，带泄漏自检）：

```bash
python3 -m agent.replay --topic ai --from 20260901 --to 20260908 --shadow pilot-2026w36
```

回归测试：`python3 -m pytest -q -p no:cacheprovider`

## 管道输出：唯一的 AnalysisSnapshot

管道输出一个对象。报告渲染、历史比较、数据库存储用的都是它 —— 不存在"展示一套、
存储另一套"。字段定义见 `references/signal-schema.md`。

读结果时先看这三处，它们决定你**允许写出什么**：

```
fetch_quality.status        complete / partial / failed
quality_gates               analysis_allowed / event_conclusions_allowed /
                            silence_conclusion_allowed / comparison_allowed /
                            window_prediction_allowed / enterprise_impact_allowed /
                            official_document_claims_allowed
各分析块的 status            ok / unknown（unknown 时该维度不得进入结论）
```

主要内容分两层：

- **事件层（结论从这里来）**：`events` 候选事件 → `extraction` 证据核验 →
  `signals` 七维信号 → `changes` 七问变化 → `dedupe` 来源归并 → `alerts` 事件告警。
  事件分两个来源层：`official_document`（正式文件，回答"正式规定了什么"）与
  `report`（人民日报报道，回答"议程如何呈现"），**分开计数，不合并**。
- **词面兼容层（只作参考特征）**：`narrative` 框架、`intensity` 七级词语强度、
  `ministry` 机构信号、`cooccurrence` 语境。这些不参与七维汇总。

## 证据纪律（违反其中任何一条，该结论不得进报告）

1. **每个关键判断都要能指回原文。** 结论旁必须有来源、原句和位置。
   拿不出证据的判断只能写进"待核验"，不能写进结论。
2. **status=unknown 就是 unknown。** 不用默认值顶替，不用"暂无数据但通常……"补全。
   无证据时不给政策阶段、不给时间窗口。
3. **采集异常是运维事件，不是政策沉默。** `fetch_quality.status != complete` 时，
   禁止写"该议题本期消失/降温/官方回避"。先说明采集缺口。
4. **只有同 topic_id、同算法版本、同采集口径的两期可比。** `comparable=False` 时，
   变化可能来自采集覆盖差异，必须写明。
5. **不承诺天数。** 有原文明确日期就引用日期；没有就写"下一项可观察事件"。
   任何"X个月内必然……"都是本框架已废弃的表述。
6. **首次观察 ≠ 首次提出。** 无历史基线时只能写"本系统首次观察到"。
7. **动机不是事实。** 报道减少、评论关闭、未跟进都只能作为可观察现象呈现；
   动机最多列为竞争假设之一，且必须并列其他解释。
8. **允许空结论。** "本期没有足够证据形成新增判断"是合格输出，不许凑数。
9. **未通过证据核验的事件不得写进结论。** `evidence_verified=false` 或
   `needs_review=true` 的，只能作为"待核验"呈现，并写出待核验的原因。
10. **转载不是独立证据。** 同一条报道被多家转载算一条原发；同体系来源
    （人民日报与人民网）不构成相互印证。
11. **暴露关系 `unmapped` 是"关系未建立"，不是"与企业无关"。** 此时不做影响推导。
12. **"未检索到相关文件"只能写成"已接入来源中没有"。** 主管部门来源（网信办、
    药监局、卫健委）当前全部取不到，绝不能因此断言"没有出台文件"。
13. **不能用报道断言正式规定的内容。** 要谈条款，必须引用正式文件及其文号。
14. **本系统未经标定。** 准确率、召回率、预警提前量全部是 UNKNOWN，
    需要人工标注与足够时长的影子运行才能得出。任何"本框架预测准确"的说法
    目前都没有依据 —— 也不得用规则自身的输出充当标准答案。
15. **董事长简报双镜头并列**：监管合规与政策窗口各自成段，空的一侧明说，
    不用另一侧填充。

## 分析顺序

```
解析主题口径 → 采集（人民日报 + 外部媒体） → 来源归并 → 目标片段切分
→ 事件抽取 → 证据核验 → 七维信号汇总 → 词面兼容特征 → 时间处理
→ 组装唯一快照 → 七问变化对比 → 报道量变化（先过数据门槛） → 存储 → 写报告
```

判断单位是**分句**。一篇同时写了"支持人工智能产业发展"和"整治违法收费"的文章，
会被拆成两个事件，后者因为分句里没有主题词而不计入本主题的方向。

## 展开阅读（按需读，不要预加载）

| 场景 | 读 |
|---|---|
| 主题口径、企业画像与暴露映射 | `references/topics.md` |
| 来源角色、独立性、采集质量标准 | `references/source-policy.md` |
| 快照字段、信号维度定义 | `references/signal-schema.md` |
| 推论规则、反例、弃权条件、已废弃的判断前提 | `references/interpretation.md` |
| 标注、回放、验收规则、当前标定状态 | `references/evaluation.md` |
| 标准报告模板 | `references/report-standard.md` |
| 董事长简报模板 | `references/report-brief.md` |

## 已修与未修

**P0（正确性）**：趋势方向反转、结果对象不统一、历史回放混入未来数据、空输入被包装成
政策状态、无关对象污染目标议题、采集失败与零命中不可区分、集体学习被升级为执行信号、
机构数与标签不一致、未标定的风险窗口乘法、政策时钟系数、叙事速度系数。

**P1（精准度）**：分句级事件抽取（主体/动作/对象/工具/方向/程序/约束/范围）、
否定与历史回顾与引述观点的区分、证据核验门槛、七维多维信号、来源归并与转载识别、
七问变化对比与替代解释、主题口径版本、企业画像与暴露映射。

**P2（时效性与来源）**：正式政策文件接入（中国政府网，含文号/发文机关/发布与生效日期/正文）、
共享文档缓存与修订检出、事件告警状态机与去重、发送回执、延迟指标、批量运行器。

**P3（标定层，已交付台账，未取得结论）**：历史回放器与泄漏自检、四条基线对比、
影子运行冻结台账与结果回填、人工标注载体、标定报告（可算的与算不了的分开呈现）。

**未修 / 未取得（不得当作已解决）**：
- **系统未经标定**：语义准确率、变化检测精确率/召回率、严重漏报、预警提前量全部 UNKNOWN，
  缺人工标注与满四周的影子运行。
- 主管部门文件来源全部取不到（网信办 521、卫健委与药监局 412）；用户已确认
  **本轮接受"只有国务院层级文件"这个覆盖**。
- 政策文件库分页 403 → 历史文件无法批量获取 → 历史政策事件集仍建不起来。
- 已接入文件来源只轮询首页，**发现延迟以月计**（实测中位数约 57 天）。
- 外部媒体时间过滤、版面实际版名。
- **尚未启动任何调度**，无真实通知发送实现（告警最多到"待投递"）。

完整清单见快照的 `known_limitations` 字段。
